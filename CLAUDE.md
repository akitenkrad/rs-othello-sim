# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## リポジトリ概要

`rs-othello-sim` は Othello (Reversi) のシミュレーション研究および強化学習実験のための Rust ワークスペースである．設計書は親 Obsidian vault の `設計書/Othello_シミュレータ設計書.md` を参照すること．

現在は **Phase 1: コアゲーム** のみ実装されている ( `crates/othello-core/`)．他のクレートは将来の Phase で追加される．

## ワークスペース構成

```
rs-othello-sim/
├── Cargo.toml                  # workspace manifest
├── crates/
│   └── othello-core/           # Phase 1 で実装済み
│       ├── src/
│       │   ├── lib.rs          # モジュール宣言と prelude
│       │   ├── color.rs        # Color enum
│       │   ├── coord.rs        # Coord 型
│       │   ├── mv.rs           # Move enum
│       │   ├── error.rs        # OthelloError ( thiserror)
│       │   ├── bitboard.rs     # Bitboard8 ( 8×8 専用)
│       │   ├── generic_board.rs # GenericBoard ( 4×4 〜 26×26)
│       │   ├── board.rs        # Board enum ( Hybrid)
│       │   ├── rules.rs        # 合法手生成・石返しの共通ロジック
│       │   └── state.rs        # GameState, GameResult
│       ├── tests/              # 統合テスト
│       └── benches/            # criterion ベンチ
└── README.md
```

## ビルド・テスト・リント

```bash
# ビルド
cargo build --workspace

# テスト
cargo test --workspace

# 単一テスト
cargo test --workspace <test_name>

# Lint ( CI で警告ゼロ強制)
cargo clippy --all-targets -- -D warnings

# フォーマット
cargo fmt
cargo fmt --check

# ベンチマーク
cargo bench

# ベンチのコンパイル確認のみ
cargo bench --no-run
```

## コーディング規約

- Rust edition = "2024"，rust-version = "1.85" 以上
- `unsafe` は使用しない
- ライブラリ層では `expect()` / `unwrap()` を避け，`Result` を返す
- パブリック型・関数には doc コメント `///` を付与
- `cargo clippy --all-targets -- -D warnings` をクリーンに保つ
- 依存クレートはワークスペースの `[workspace.dependencies]` に集約し，各クレートで `serde.workspace = true` のように継承

## Phase 1 の主要設計判断

- **Hybrid 盤面**: 8×8 では `Bitboard8` ( `u64 × 2`) で高速化．$4 \times 4$ から $26 \times 26$ までは `GenericBoard` ( `Vec<Option<Color>>`)
- **Bitboard レイアウト**: `bit_index = row * 8 + col` ( 行 0 列 0 = bit 0)
- **8 方向シフト**: 列マスク ( A 列・H 列) で wrap-around を防ぐ．5 回反復で連鎖石マスクを構築
- **同値性保証**: 8×8 では `Bitboard8` と `GenericBoard` の合法手・石返しが完全一致することをプロパティテストで検証

## Markdown ファイル規約

- すべての `.md` ファイルの末尾に以下のフッタを必ず付与:

```
---
*This file was generated by Claude Code.*
```

- `.md` ファイル内では日本語の句読点として `，` と `．` を使用 ( `、` `。` は不可)

## Git ワークフロー

- `git init` はユーザが行う ( Claude は実行しない)
- 自動コミットは行わない

## 将来の Phase で追加予定のクレート

設計書 §10 に従い段階的に実装する．

| Phase | 追加クレート |
|---|---|
| Phase 2 | `othello-player`, `othello-engine`, `othello-io`, `othello-cli` |
| Phase 3 | `othello-tui`，WTHOR 読込，JSONL ロガー |
| Phase 4 | `othello-rl`, `othello-py`，バッチ self-play |
| Phase 5 | 外部エンジン連携 ( Edax/Egaroucid)，可視化ツール |

---
*This file was generated by Claude Code.*
